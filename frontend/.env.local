# Backend proxy destination — read by next.config.js at server startup (NOT baked
# into the client bundle). The browser always calls relative /api/* paths and
# the Next.js rewrite forwards them here.
BACKEND_URL=http://localhost:8000
